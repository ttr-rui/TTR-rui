# 需求分析

## 功能需求
给定原文文件和抄袭版论文文件，计算重复率并写入答案文件。

## 输入
命令行传入 3 个参数：
1. 论文原文的绝对路径
2. 抄袭版论文的绝对路径
3. 答案文件的绝对路径

## 输出
答案文件中写入一个浮点型重复率，保留两位小数（如 `0.85`）。

## 约束
- 运行时间 < 5 秒
- 占用内存 < 2048 MB
- 不联网
- 不读写除 3 个参数外的其他文件
- 异常不能导致程序崩溃退出
- 使用 Python3 实现，入口文件名必须为 `main.py`
